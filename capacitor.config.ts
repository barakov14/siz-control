import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sizcontrol.app',
  appName: 'СИЗ.Контрол',
  webDir: 'dist/apps/siz-control-mobile/browser',
};

export default config;
